VERCEL_AI_GATEWAY_KEY=
DATABASE_URL="file:./dev.db"
# sqlite | postgresql | mysql | sqlserver  (used by scripts/set-db-provider.mjs)
DATABASE_PROVIDER="sqlite"
JWT_SECRET=
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# Used to protect the cron cleanup endpoint — set a long random secret
CRON_SECRET=
